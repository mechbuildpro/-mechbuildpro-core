export { default as Component } from './Component';
export { default as Form } from './Form';
export { useDesignManagement } from './logic';

// Çizim raporları
export const exportDrawingReport = () => {
  // Çizim raporu oluşturma mantığı
};

// Model raporları
export const exportModelReport = () => {
  // Model raporu oluşturma mantığı
};

// Revizyon raporları
export const exportRevisionReport = () => {
  // Revizyon raporu oluşturma mantığı
};

// Onay raporları
export const exportApprovalReport = () => {
  // Onay raporu oluşturma mantığı
};

// Dokümantasyon raporları
export const exportDocumentationReport = () => {
  // Dokümantasyon raporu oluşturma mantığı
}; 