export { default as Component } from './Component';
export { default as Form } from './Form';
export { useResourceManagement } from './logic';

// Ekipman raporları
export const exportEquipmentReport = () => {
  // Ekipman raporu oluşturma mantığı
};

// Malzeme raporları
export const exportMaterialReport = () => {
  // Malzeme raporu oluşturma mantığı
};

// İş gücü raporları
export const exportWorkforceReport = () => {
  // İş gücü raporu oluşturma mantığı
};

// Araç raporları
export const exportVehicleReport = () => {
  // Araç raporu oluşturma mantığı
};

// Bina sistemleri raporları
export const exportBuildingSystemReport = () => {
  // Bina sistemleri raporu oluşturma mantığı
};

// HVAC sistemleri raporu
export const exportHVACReport = () => {
  // HVAC sistemleri raporu oluşturma mantığı
};

// Elektrik sistemleri raporu
export const exportElectricalReport = () => {
  // Elektrik sistemleri raporu oluşturma mantığı
};

// Tesisat sistemleri raporu
export const exportPlumbingReport = () => {
  // Tesisat sistemleri raporu oluşturma mantığı
};

// Yangın sistemleri raporu
export const exportFireReport = () => {
  // Yangın sistemleri raporu oluşturma mantığı
}; 